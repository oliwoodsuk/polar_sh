## Unreleased

- Pin requests to the `2026-04` Polar API version via the `Polar-Version` header, configurable with `config.api_version`
- Declare `base64` as a dependency so the gem loads on Ruby 3.4+

## 0.2.0

- Add `Polar::Webhook`
- Add more resources

## 0.1.0

- Initial release
